# <Model name>

One paragraph: the economic problem, the stages in the period, and the
methods used (EGM variant, upper envelope, discrete choices).

Run from the repo root:

    python -m examples.<model>.run

See examples/README.md for the standard layout this example follows.
