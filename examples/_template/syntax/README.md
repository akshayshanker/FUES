# syntax/ — the model definition

Calibration, numerical settings, period/nest wiring, spec_factory.yaml,
and one folder per stage under stages/<stage>/ (stage YAML + methods
YAML). With several parameterisations, use one registry folder per
parameterisation, each with its own callables.py.
