# Durables example — developer notes

Example-specific documentation for developers and AI sessions lives here:
the solution-dict layout (`solution_scheme.md`), the estimation output
contract (`estimation_outputs.md`), and the periodic-restart mechanism for
long estimation runs (`periodic_restart.md`).

The user-facing documentation page for this example is
`docs/examples/continuous_housing_model.md` at the repo root, rendered at
https://akshayshanker.github.io/FUES/examples/continuous_housing_model/.
