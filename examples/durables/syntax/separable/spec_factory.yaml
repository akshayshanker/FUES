# spec_factory.yaml — durables, separable utility, females (baseline)
stages:
  tenure:
    calibration:
      all: [calibration/main, $draw]
    settings:
      all: [settings/default, $draw]
    methods:
      all: [stages/tenure/tenure_methods]

  keeper_cons:
    calibration:
      all: [calibration/main, $draw]
    settings:
      all: [settings/default, $draw]
    methods:
      all: [stages/keeper_cons/keeper_cons_methods]

  adjuster_cons:
    calibration:
      all: [calibration/main, $draw]
    settings:
      all: [settings/default, $draw]
    methods:
      all: [stages/adjuster_cons/adjuster_cons_methods, $method_switch]
