# v3: slot-range payload — one bundle per row; $draw in calibration
# Used as: --slot-range @experiments/retirement/timing_deltas.yaml
- draw:
    calibration: { delta: 0.25 }
- draw:
    calibration: { delta: 0.5 }
- draw:
    calibration: { delta: 1.0 }
- draw:
    calibration: { delta: 2.0 }
